# Environment
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.coverage

# Database & data (never commit)
data/

# Claude Code settings (local only)
.claude/

.stuff

# Frontend build
ui/node_modules/
ui/dist/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# A1.Flex hunter config (contains OCI credentials)
.env.hunt

# Insperity sync token (contains Vaultic JWT)
.insperity_token
.insperity_refresh_token
.insperity_curl.txt

# Voya sync token + cURL (contains Vaultic JWT + live Voya session cookies)
.voya_token
.voya_refresh_token
.voya_curl.txt
.voya_raw.json

# EveryDollar JSON imports (raw API responses — not for version control)
imports/

# Design reference screenshots (local only)
screenshots/

# Voya capture artifacts (contain session tokens) — never commit
voya_grab.js
.voya_targets.json
voya_data.json
.voya_raw.json
